apiVersion: v2
kind: Service

metadata:
  name: decisiontree-service

spec:
  type: NodePort

  selector:
    app: decisiontree

  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30007
    